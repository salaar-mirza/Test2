namespace ServiceArchitecture.Input.Events
{
    /// <summary>
    /// Published when the player initiates the primary action (e.g., left-click).
    /// This is a struct to avoid garbage collection.
    /// </summary>
    public struct PrimaryActionStartedEvent
    {
    }
}